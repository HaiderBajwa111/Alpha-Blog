<h1 class="text-center mt-2">Category: <%= @category.name %></h1>
<% if logged_in?  &&  current_user.admin? %>
<div class="text-center mt-2">
<%= link_to 'Edit category name' , edit_category_path(@category),  class: "mx-auto  btn btn-outline-primary" %>
</div>
<%end %>
<h1 class="text-center mt-2">Articles</h1>
<%= render 'articles/articles'%>